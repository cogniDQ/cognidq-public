This file keeps the `diagrams/` directory tracked by git until real
diagrams are added. Most architecture/flow diagrams are inlined as
Mermaid in the relevant markdown page (e.g. `../architecture.md`);
this folder is only for diagrams too complex for inline Mermaid.
