[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
markers =
    integration: marks tests as integration tests (require a live database / services)
    e2e: marks tests as end-to-end tests (require full stack)
